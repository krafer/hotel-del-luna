{
  "name": "cdc-webapp",
  "version": "1.0.0",
  "description": "Hotel reservation website with an Express and Firebase Firestore backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node --check server.js && node --check db.js && node --check reservation.js && node --check contact.js && node --check nav.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.2.1",
    "firebase-admin": "^14.4.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
